/* const actions = (dispatch) => ({
  increment: () => dispatch({ type: "counter/increment" }),
  incrementAmmount: ({ payload }) =>
    dispatch({ type: "counter/incrementAmmount", payload }),
});

export default actions;
 */
